# AI Usage Releases

Public release artifacts for the private `JialinLiu-codedance/ai-usage` source repository.

This repository is used by GitHub Actions to publish:

- macOS `.dmg` installers
- Tauri updater bundles and signatures
- `latest.json` metadata for in-app updates
